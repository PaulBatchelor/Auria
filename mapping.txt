Things to map:
Realtime input 
X_L joystick mapping 
Y_L joystick mapping 

X_R joystick mapping 
Y_R joystick mapping 

Ball X pos (set)
Ball Y pos (set)
Ball Z pos (set)

bgColor R
bgColor G
bgColor B

ballColor R
ballColor G
ballColor B

lineColor R
lineColor G
lineColor B

left trigger
blue X
red B
yellow Y

tick gen
